import React from 'react'
import LoaderSrc from '../../assets/loader.gif'

const Loader = props => (
  <div>
    <img style={{ width: 160 }} src={LoaderSrc} alt='Loader Icon' />
  </div>
)

export default Loader
